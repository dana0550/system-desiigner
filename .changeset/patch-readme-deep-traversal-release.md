---
"sdx-cli": patch
---

Publish the deep README traversal and README-check reliability fixes in a new patch release.

Highlights:
- narrative README generation from deep repo documentation traversal
- local `repo add` now updates repo sync timestamps for reliable `docs readme --check`
